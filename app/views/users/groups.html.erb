<h3>My Groups</h3>
<% @groups.each do |group| %>
  <h4><%= group.topic %></h4>
  <p><%= group.description %></p>
  <%= render partial: 'groups/leave_group_options', locals: {group: group} %>
<% end %>