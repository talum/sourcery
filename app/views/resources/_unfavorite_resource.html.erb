<%= link_to favorite_path(resource.favorites.where(user_id: current_user.id).first), method: :delete, remote: true, class: "btn btn-primary btn-xs unfavorite" do %>
  <i class="fa fa-magic"></i> Unfavorite
<% end %>