<div>
  <%= render 'public_activity/activity_header', {a: a} %>
  <% if a.trackable %>
    left <%= link_to a.trackable.topic, group_path(a.trackable) %>.
  <% else %>
    left a group that has since been deleted. 
  <% end %>
</div>