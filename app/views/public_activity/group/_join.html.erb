<div>
  <%= render 'public_activity/activity_header', {a: a} %>
  <% if a.trackable %>
    joined <%= link_to a.trackable.topic, group_path(a.trackable) %>.
  <% else %>
    joined a group that has since been deleted. 
  <% end %>
</div>