<div>
  <%= render 'public_activity/activity_header', {a: a} %>
  <% if a.trackable %>
    created <%= link_to a.trackable.topic, group_path(a.trackable) %>.
  <% else %>
    created a group that has since been deleted.
  <% end %>
</div>