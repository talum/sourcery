<p>Group Topic: <%= @group.topic %></p>

<p>Resources</p>
<ul>
<% @group.resources.each do |resource| %>
  <li><%= resource.title %>: 
  <%= link_to "#{resource.link}", resource.link %></li>
<% end %>
</ul>

<%= button_to "delete", @group, method: :delete %>

<h3>Add New Resource</h3>

<%= render 'resources/form', group_id: @group.id %>